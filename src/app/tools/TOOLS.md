# Tools

Tools are tiny helpfull features that could be used in the daily life. This readme explains how one could add a new tool.

## Add a new tool

Add a new component through:

`ng generate component tools/my-new.tool`

Add this new entry to the `ToolRegistryEntry` in [tools.registry.ts](tools.registry.ts)
